export const environment = {
  production: false,
  //the auth intercepter will add the username before the rest of the path 
  // if the path does not start with "/user"
  baseUrl: "http://localhost:8080"
};

